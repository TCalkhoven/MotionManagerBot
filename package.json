{
  "name": "motionmanagerbot",
  "version": "1.0.0",
  "description": "This is the motion manager bot. It is a telegram bot that will manage the votes on a motion.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thijs Calkhoven",
  "license": "ISC",
  "dependencies": {
    "node-telegram-bot-api": "^0.30.0"
  },
  "devDependencies": {
    "eslint-config-standard": "^12.0.0"
  }
}
